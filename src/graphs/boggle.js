let words = ["GEEKS", "FOR", "QUIZ", "GO"];
let graph = [ ['G','I','Z'],
              ['U','E','K'],
              ['Q','S','E'] ];

let boggle = (graph, targets) => {
  let Answer = [];

  return Answer;
};

let work = boggle(graph, words);

console.log(work);